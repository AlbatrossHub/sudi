<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_sudi_sync_prune" model="ir.cron">
            <field name="name">Sudi Sync: prune the change log</field>
            <field name="model_id" ref="model_sudi_sync_change"/>
            <field name="state">code</field>
            <field name="code">model._cron_prune()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(datetime.now().replace(hour=1, minute=30, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="active" eval="True"/>
        </record>

        <record id="ir_cron_sudi_upload_prune" model="ir.cron">
            <field name="name">Sudi Sync: collect unclaimed uploads</field>
            <field name="model_id" ref="model_sudi_upload"/>
            <field name="state">code</field>
            <field name="code">model._cron_prune()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(datetime.now().replace(hour=2, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
